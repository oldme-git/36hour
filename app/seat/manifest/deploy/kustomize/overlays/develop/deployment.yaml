apiVersion: apps/v1
kind: Deployment
metadata:
  name: seat
spec:
  template:
    spec:
      containers:
        - name : main
          image: registry.cn-hangzhou.aliyuncs.com/36hour/seat_handle:latest
          volumeMounts:
            - name: conf-volume
              mountPath: /app/config.yaml
              subPath: seat_handle.yaml